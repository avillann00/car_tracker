'use client'

export default function NavBar(){
  return(
    <div className='bg-gray-200 flex flex row items-center justify-center w-screen p-2 fixed bottom-0 left-0 w-full z-50'>
      <button 
        className='text-black hover:text-blue-500'
      >
        Contact
      </button>
    </div>
  )
}
